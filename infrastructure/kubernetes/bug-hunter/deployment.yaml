apiVersion: apps/v1
kind: Deployment
metadata:
  name: bug-hunter
  namespace: codesentinel
  labels:
    app: bug-hunter
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bug-hunter
  template:
    metadata:
      labels:
        app: bug-hunter
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8003"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: bug-hunter
          image: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/codesentinel/bug-hunter:latest
          ports:
            - containerPort: 8003
          envFrom:
            - secretRef:
                name: codesentinel-secrets
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 10
