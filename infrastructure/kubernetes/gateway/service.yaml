apiVersion: v1
kind: Service
metadata:
  name: gateway-svc
  namespace: codesentinel
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: LoadBalancer   # External IP — webhook URL points here
