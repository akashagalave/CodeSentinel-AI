apiVersion: v1
kind: Service
metadata:
  name: gateway-svc
  namespace: codesentinel
  labels:
    app: gateway
    app.kubernetes.io/part-of: codesentinel
spec:
  selector:
    app: gateway
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: LoadBalancer