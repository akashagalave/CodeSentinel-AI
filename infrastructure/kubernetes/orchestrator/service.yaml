apiVersion: v1
kind: Service
metadata:
  name: orchestrator-svc
  namespace: codesentinel
  labels:
    app: orchestrator
    app.kubernetes.io/part-of: codesentinel
spec:
  selector:
    app: orchestrator
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP