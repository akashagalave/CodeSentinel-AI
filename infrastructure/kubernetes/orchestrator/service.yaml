apiVersion: v1
kind: Service
metadata:
  name: orchestrator-svc
  namespace: codesentinel
spec:
  selector:
    app: orchestrator
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP   # Internal only — gateway calls this
